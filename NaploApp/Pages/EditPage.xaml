<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NaploApp.Pages.EditPage"
             Title="EditPage">
    <VerticalStackLayout>
        <Label Text="Leírás"/>
        <Entry Placeholder="Írd be a leírást" Text="{Binding Post.Details}"/>

        <Button Text="Mentés" BindingContext="{Binding C_Edit}" Margin="0, 10, 0, 0"/>
    </VerticalStackLayout>
</ContentPage>